torch>=2.1.0
torchvision>=0.16.0
transformers>=4.40.0
huggingface-hub>=0.19.0
opencv-python>=4.7.0
numpy>=1.24.0
Pillow>=9.5.0
transparent-background>=1.3.0
scipy>=1.10.0
# Depth Pro (Apple MLRC 2024) — best-in-class boundary-sharp monocular depth
# Install manually: pip install git+https://github.com/apple/ml-depth-pro
# OR the node will attempt HuggingFace transformers fallback automatically.
# depth-pro  # uncomment if installed via PyPI mirror
